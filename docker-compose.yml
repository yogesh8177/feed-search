version: "3.7"
services: 
  web: 
    build: .
    command: npm start
    ports:
     - "8000:8000"
    
  ui:
    build: ./ui
    command: ng e2e --protractorConfig=docker.protractor.conf.js
    links: 
      - web
    depends_on: 
      - web
    